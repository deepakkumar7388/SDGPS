package com.example.digitalpass.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand Colors (preserved from original app) ──
val BrandPrimary = Color(0xFF7E57FF)       // Purple — buttons, accents
val BrandSecondary = Color(0xFF4B72FA)     // Blue — links, secondary actions
val BrandDark = Color(0xFF052E92)          // Dark navy — sidebar icons, emphasis

// ── Gradient endpoints ──
val GradientStart = Color(0xFF4B72FA)
val GradientEnd = Color(0xFF7E57FF)

// ── Background tones ──
val ScreenBgStart = Color(0xFFF6F9FF)
val ScreenBgEnd = Color(0xFFE3EDFB)
val SurfaceWhite = Color(0xFFFFFFFF)

// ── Text colors ──
val TextPrimary = Color(0xFF1A1A1A)
val TextSecondary = Color(0xFF333333)
val TextMuted = Color(0xFF757575)
val TextHint = Color(0xFF8F9CAE)

// ── Gateway / Cinematic colors ──
val GatewayBg = Color(0xFF0A0E1A)
val NeonCyan = Color(0xFF00F2FE)
val AccessGreen = Color(0xFF2AF598)

// ── Status colors ──
val ErrorRed = Color(0xFFD32F2F)
val SuccessGreen = Color(0xFF2AF598)
val WarningAmber = Color(0xFFFFC107)

// ── Divider & border ──
val DividerColor = Color(0xFFEEEEEE)
val InputBorder = Color(0xFFE0E6F2)
